#include <iostream>
#include <string>
#include <vector>

using namespace std;

void init() {
    ios_base::sync_with_stdio(false);
    cin.tie(0);
    cout.tie(0);
}
int solution(int k, vector<vector<int>> dungeons) {
    int answer = -1;
    return answer;
}