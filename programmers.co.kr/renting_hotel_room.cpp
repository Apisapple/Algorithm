#include <string>
#include <vector>

using namespace std;

int solution(vector<vector<string>> book_time) {
  int answer = 0;

  vector<pair<int, int>> book_time_info = getPairFromVector(book_time);

  return answer;
}

vector<pair<int, int>> getPairFromVector(vector<vector<string>> vector_info) {
  vector<pair<int, int>> result;

  for (vector<string> info : vector_info) {
  }

  return result;
}